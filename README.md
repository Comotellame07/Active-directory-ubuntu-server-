Para que este script funcione es necesario poner el comando
chmod +x examen.sh
en la carpeta en la que se encuentre el archivo examen.sh

Este script sirve para crear, configurar y administrar un servidor de active directory en ubuntu server

